# src/

This directory holds the payments-service application code.

> Placeholder: in a real repo this is existing project code, left untouched by
> the Kiro knowledge-base setup. It is created here only so the directory exists
> in version control. Replace it with the service implementation.
